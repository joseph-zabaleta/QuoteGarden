'use strict';


const { isCached, isCachedAll } = require('../../../api/middlewares/cache.js');

describe('Testing Cache Middleware Functionaility: ', () => {

    it('Should work', () => {

        expect(1).toEqual(1);

    });

});